#include<bits/stdc++.h>
using namespace std;
int main(){
    float a, b, c, product;

    cout << "enter three float numbers:";
    cin >> a >> b >> c;

    product = a * b * c;
    cout << "the product of the three number is:" << product << endl;
    return 0;
}